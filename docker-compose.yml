services:
  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: rocketseat
      MYSQL_ROOT_PASSWORD: root
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
    driver: local
